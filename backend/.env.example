# Owner: FastAPI server. Optional; defaults to development at process runtime.
# Allowed values: development, test, production.
ENVIRONMENT=development

# Owner: FastAPI server. Optional until CORS configuration (Task 4.3).
# Format: one absolute HTTP(S) origin without a path, query, or fragment.
FRONTEND_ORIGIN=http://localhost:3000

# Owner: FastAPI server. Optional at startup; required only for database operations.
# SQLAlchemy URL kept server-only. Supply privately; never commit a real value.
DATABASE_URL=
