# Safe local defaults reserved for later settings and CORS tasks.
ENVIRONMENT=development
FRONTEND_ORIGIN=http://localhost:3000

# Deferred until database setup. Supply this privately later; never commit a real value.
DATABASE_URL=
