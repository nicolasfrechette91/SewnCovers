# SewnCovers backend

This directory is reserved for the Python and FastAPI service. The service scaffold is the next backend foundation task and is intentionally not implemented as part of the repository baseline.

The backend will own validation, catalogue and saved-design endpoints, and the only connection to PostgreSQL on Neon. See [`../docs/PROJECT_PROGRESS.md`](../docs/PROJECT_PROGRESS.md) for status.
