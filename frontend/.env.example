# Public browser configuration embedded in the frontend bundle.
# Never place credentials, API keys, database URLs, or other secrets in NEXT_PUBLIC_ variables.
NEXT_PUBLIC_API_URL=http://localhost:8000
