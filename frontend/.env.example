# Owner: Next.js browser application. Required when making API requests.
# Format: absolute HTTP(S) URL. Read and embedded at build time, not at page runtime.
# Public value only: never add credentials, API keys, or database URLs here.
NEXT_PUBLIC_API_URL=http://localhost:8000
